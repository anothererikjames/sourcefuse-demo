$kind: http-example
request:
  url: "{{baseUrl}}/v1/charges"
  method: POST
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "id": "ch_demo_001",
        "amount": 25000,
        "currency": "usd",
        "status": "succeeded"
      }
order: 1000
