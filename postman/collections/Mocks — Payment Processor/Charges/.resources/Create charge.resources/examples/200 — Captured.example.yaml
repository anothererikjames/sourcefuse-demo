$kind: http-example
request:
  url: "{{baseUrl}}/v1/charges"
  method: POST
response:
  statusCode: 200
  statusText: OK
  body:
    type: raw
    content: |-
      {
        "id": "ch_demo_001",
        "amount": 25000,
        "currency": "usd",
        "status": "succeeded"
      }
    options:
      raw:
        language: json
order: 1000
