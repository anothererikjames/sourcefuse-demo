$kind: http-example
request:
  url: "{{baseUrl}}/v1/charges"
  method: POST
response:
  statusCode: 503
  statusText: OK
  body:
    type: raw
    content: |-
      {
        "error": {
          "type": "api_error",
          "message": "Payment provider is unavailable"
        }
      }
    options:
      raw:
        language: json
order: 1000
