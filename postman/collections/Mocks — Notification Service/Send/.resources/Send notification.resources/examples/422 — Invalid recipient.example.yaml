$kind: http-example
request:
  url: "{{baseUrl}}/v1/notifications"
  method: POST
response:
  statusCode: 422
  statusText: OK
  body:
    type: raw
    content: |-
      {
        "error": {
          "code": "validation_error",
          "fields": [
            {
              "field": "to",
              "rule": "email"
            }
          ]
        }
      }
    options:
      raw:
        language: json
order: 1000
