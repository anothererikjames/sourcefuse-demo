$kind: http-example
request:
  url: "{{baseUrl}}/customers/{{customerId}}/kyc"
  method: POST
response:
  statusCode: 201
  statusText: OK
  body:
    type: raw
    content: |-
      {
        "caseId": "kyc_ai_reject",
        "state": "rejected",
        "reason": "document_expired",
        "factors": [
          {
            "type": "document",
            "result": "fail"
          }
        ]
      }
    options:
      raw:
        language: json
order: 1000
