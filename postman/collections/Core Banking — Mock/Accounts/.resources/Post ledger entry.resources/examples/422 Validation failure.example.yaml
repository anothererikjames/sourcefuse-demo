$kind: http-example
request:
  url: "{{baseUrl}}/v2/accounts/:accountId/ledger"
  method: POST
  pathVariables:
    accountId: ""
response:
  statusCode: 422
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "error": {
          "code": "validation_error",
          "fields": [
            {
              "field": "amount",
              "rule": "positive"
            }
          ]
        }
      }
order: 2000
