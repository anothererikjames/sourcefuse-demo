$kind: http-example
request:
  url: "{{baseUrl}}/v2/accounts/{{accountId}}/ledger"
  method: POST
response:
  statusCode: 201
  statusText: OK
  body:
    type: raw
    content: |-
      {
        "id": "led_001",
        "balanceAfter": {
          "amount": "1350.42",
          "currency": "USD"
        }
      }
    options:
      raw:
        language: json
order: 1000
