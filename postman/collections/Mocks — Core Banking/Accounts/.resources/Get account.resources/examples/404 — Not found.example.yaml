$kind: http-example
request:
  url: "{{baseUrl}}/v2/accounts/{{accountId}}"
  method: GET
response:
  statusCode: 404
  statusText: OK
  body:
    type: raw
    content: |-
      {
        "error": {
          "code": "account_not_found",
          "message": "Account does not exist"
        }
      }
    options:
      raw:
        language: json
order: 1000
