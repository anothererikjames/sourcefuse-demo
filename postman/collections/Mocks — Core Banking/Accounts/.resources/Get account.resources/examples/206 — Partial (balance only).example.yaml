$kind: http-example
request:
  url: "{{baseUrl}}/v2/accounts/{{accountId}}"
  method: GET
response:
  statusCode: 206
  statusText: OK
  headers:
    - key: X-Partial-Reason
      value: core-ledger-degraded
  body:
    type: raw
    content: |-
      {
        "id": "acc_4f3b1e22",
        "balance": {
          "amount": "1250.42",
          "currency": "USD"
        },
        "_partial": true
      }
    options:
      raw:
        language: json
order: 1000
