$kind: http-request
url: "{{baseUrl}}/projects/:projectId/tasks"
method: POST
headers:
  Content-Type: application/json
  Accept: application/json
pathVariables:
  - key: projectId
    value: b9574e3d-dc9a-14da-6ddc-f5c5cf7d4e24
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string",
            "format": "uuid"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: json
  content: |-
    {
      "title": "string",
      "description": "string",
      "status": "todo",
      "priority": "medium",
      "assignee": "string",
      "dueDate": "1994-07-16"
    }
  $type: |-
    {
      "type": "object",
      "properties": {
        "title": {
          "type": "string"
        },
        "description": {
          "type": "string"
        },
        "status": {
          "type": "string",
          "enum": [
            "todo",
            "in_progress",
            "blocked",
            "done"
          ]
        },
        "priority": {
          "type": "string",
          "enum": [
            "low",
            "medium",
            "high",
            "urgent"
          ]
        },
        "assignee": {
          "type": "string"
        },
        "dueDate": {
          "type": "string",
          "format": "date"
        }
      },
      "required": [
        "title"
      ]
    }
examples: ./.resources/Create task.resources/examples
order: 2000
