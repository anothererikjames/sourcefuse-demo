$kind: http-request
url: "{{baseUrl}}/tasks/:taskId"
method: PATCH
headers:
  Content-Type: application/json
  Accept: application/json
pathVariables:
  - key: taskId
    value: b9574e3d-dc9a-14da-6ddc-f5c5cf7d4e24
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string",
            "format": "uuid"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: json
  content: |-
    {
      "title": "string",
      "description": "string",
      "status": "done",
      "priority": "medium",
      "assignee": "string",
      "dueDate": "1970-11-20"
    }
  $type: |-
    {
      "type": "object",
      "properties": {
        "title": {
          "type": "string"
        },
        "description": {
          "type": "string"
        },
        "status": {
          "type": "string",
          "enum": [
            "todo",
            "in_progress",
            "blocked",
            "done"
          ]
        },
        "priority": {
          "type": "string",
          "enum": [
            "low",
            "medium",
            "high",
            "urgent"
          ]
        },
        "assignee": {
          "type": "string"
        },
        "dueDate": {
          "type": "string",
          "format": "date"
        }
      }
    }
examples: ./.resources/Update task.resources/examples
order: 2000
